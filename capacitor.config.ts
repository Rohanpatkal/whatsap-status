import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.whatsappgallery',
  appName: 'whatsapp-gallery',
  webDir: 'build'
};

export default config;
